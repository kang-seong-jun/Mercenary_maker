{
  "name": "mercenary-maker:-ai-chronicles",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && npx firebase deploy --non-interactive",
    "deploy:ci": "dotenv -e .env -- npm run build && dotenv -e .env -- npx firebase deploy --non-interactive"
  },
  "dependencies": {
    "@google/genai": "^1.19.0",
    "firebase": "^12.3.0",
    "react": "^19.1.1",
    "react-dom": "^19.1.1"
  },
  "devDependencies": {
    "@types/node": "^22.14.0",
    "dotenv-cli": "^10.0.0",
    "firebase-tools": "^14.17.0",
    "typescript": "~5.8.2",
    "vite": "^6.2.0"
  }
}
